// Auto-generated by generate_serialise_for_types.py
// Reads

int8_t DgStreamReadInt8(DgStream *context, DgError *error) {
	/**
	 * Read a int8_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The int8_t which was read from the stream
	 */
	
	int8_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


uint8_t DgStreamReadUInt8(DgStream *context, DgError *error) {
	/**
	 * Read a uint8_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The uint8_t which was read from the stream
	 */
	
	uint8_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


int16_t DgStreamReadInt16(DgStream *context, DgError *error) {
	/**
	 * Read a int16_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The int16_t which was read from the stream
	 */
	
	int16_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


uint16_t DgStreamReadUInt16(DgStream *context, DgError *error) {
	/**
	 * Read a uint16_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The uint16_t which was read from the stream
	 */
	
	uint16_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


int32_t DgStreamReadInt32(DgStream *context, DgError *error) {
	/**
	 * Read a int32_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The int32_t which was read from the stream
	 */
	
	int32_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


uint32_t DgStreamReadUInt32(DgStream *context, DgError *error) {
	/**
	 * Read a uint32_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The uint32_t which was read from the stream
	 */
	
	uint32_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


int64_t DgStreamReadInt64(DgStream *context, DgError *error) {
	/**
	 * Read a int64_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The int64_t which was read from the stream
	 */
	
	int64_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


uint64_t DgStreamReadUInt64(DgStream *context, DgError *error) {
	/**
	 * Read a uint64_t from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The uint64_t which was read from the stream
	 */
	
	uint64_t data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


float DgStreamReadFloat32(DgStream *context, DgError *error) {
	/**
	 * Read a float from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The float which was read from the stream
	 */
	
	float data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0.0f;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}


double DgStreamReadFloat64(DgStream *context, DgError *error) {
	/**
	 * Read a double from the stream
	 * 
	 * @param context Stream to read from
	 * @param error Any error which occured will be stored here, may be NULL
	 * @return The double which was read from the stream
	 */
	
	double data;
	
	DgError error_code = DgStreamRead(context, sizeof data, &data);
	
	if (error_code) {
		if (error) {
			*error = error_code;
		}
		
		return 0.0;
	}
	
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return data;
}

// Writes

DgError DgStreamWriteInt8(DgStream *context, int8_t data) {
	/**
	 * Write a Int8 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Int8 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteUInt8(DgStream *context, uint8_t data) {
	/**
	 * Write a UInt8 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The UInt8 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteInt16(DgStream *context, int16_t data) {
	/**
	 * Write a Int16 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Int16 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteUInt16(DgStream *context, uint16_t data) {
	/**
	 * Write a UInt16 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The UInt16 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteInt32(DgStream *context, int32_t data) {
	/**
	 * Write a Int32 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Int32 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteUInt32(DgStream *context, uint32_t data) {
	/**
	 * Write a UInt32 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The UInt32 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteInt64(DgStream *context, int64_t data) {
	/**
	 * Write a Int64 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Int64 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteUInt64(DgStream *context, uint64_t data) {
	/**
	 * Write a UInt64 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The UInt64 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteFloat32(DgStream *context, float data) {
	/**
	 * Write a Float32 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Float32 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}


DgError DgStreamWriteFloat64(DgStream *context, double data) {
	/**
	 * Write a Float64 to a stream.
	 * 
	 * @param context Stream object
	 * @param data The Float64 to write
	 * @return Error code
	 */
	 
	if (DgStreamIsSwappingEndian(context)) {
		DgMemorySwapEndian(&data, sizeof data);
	}
	
	return DgStreamWrite(context, sizeof data, &data);
}

